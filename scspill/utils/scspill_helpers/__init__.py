"""Shared spillover layer, plus one subpackage per model.

One estimator = one package: configuration, panel setup, the two-step MCMC
samplers, effect computation, inference, diagnostics, plotting, and the
pipeline that orchestrates them.
"""
